<Window x:Class="OrdersService.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:OrdersService.Views" xmlns:viewmodels="clr-namespace:OrdersService.ViewModels"
        d:DataContext="{d:DesignInstance Type=viewmodels:MainWindowViewModel}"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Window.Resources>
        <ResourceDictionary>
            <Style TargetType="{x:Type Button}"  x:Key="btnStyle"> 
                <Setter Property="Padding" Value="5"/>
            </Style>
        </ResourceDictionary>
    </Window.Resources>
    <Grid>
        <StackPanel>
            <StackPanel.Resources>
                <Style TargetType="{x:Type Button}" BasedOn="{StaticResource btnStyle}">
                    <Setter Property="Margin" Value="5"/>
                </Style>
            </StackPanel.Resources>
            <Button Content="Login" Command="{Binding LoginCommand}" HorizontalAlignment="Center" />
            <Button Content="Get Orders" Command="{Binding GetOrdersCommand}" HorizontalAlignment="Center" />
            <Button Content="Close" Command="{Binding CloseCommand}" HorizontalAlignment="Center" />
        </StackPanel>
    </Grid>
</Window>
